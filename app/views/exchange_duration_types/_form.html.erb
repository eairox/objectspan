<%= form_for(@exchange_duration_type) do |f| %>
  <% if @exchange_duration_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exchange_duration_type.errors.count, "error") %> prohibited this exchange_duration_type from being saved:</h2>

      <ul>
      <% @exchange_duration_type.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
