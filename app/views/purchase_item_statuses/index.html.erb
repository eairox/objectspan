<h1>Listing purchase_item_statuses</h1>

<table>
  <tr>
    <th>Name</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @purchase_item_statuses.each do |purchase_item_status| %>
  <tr>
    <td><%= purchase_item_status.name %></td>
    <td><%= link_to 'Show', purchase_item_status %></td>
    <td><%= link_to 'Edit', edit_purchase_item_status_path(purchase_item_status) %></td>
    <td><%= link_to 'Destroy', purchase_item_status, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Purchase item status', new_purchase_item_status_path %>
