<%= form_for(@purchase_status) do |f| %>
  <% if @purchase_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase_status.errors.count, "error") %> prohibited this purchase_status from being saved:</h2>

      <ul>
      <% @purchase_status.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
